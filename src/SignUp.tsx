import React from 'react';
import {View,Text} from 'react-native';
const SignUp=()=>{
    return (
        <View style={{flex:1,}}>
            <Text style={{fontSize:40,alignSelf:'center'}}>Ecommerce</Text>
        </View>
    );
};

export default SignUp;
